let arr = [1,2,3,4,5];
let output1 = arr.reduce((acc, x) => acc + x);
console.log("sum of arr " + output1);

// syntax: array.reduce(function(total, currentValue, currentIndex, arr), initialValue)
